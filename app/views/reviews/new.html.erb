<div class="container">
  <%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :rating, as: :radio_buttons, :collection => (0..5) %>
    <%= f.input :content %>
    <%= f.submit %>
  <% end %>
</div>
